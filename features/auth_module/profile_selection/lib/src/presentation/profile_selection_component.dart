import 'package:flutter/material.dart';

class ProfileSelectionComponent extends StatelessWidget {
  const ProfileSelectionComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
